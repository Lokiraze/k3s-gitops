config: |
  mqtt:
    host: eclipse-mosquitto
    topic_prefix: frigate
  database:
    path: /data/frigate.db
  detectors:
    coral:
      type: edgetpu
      device: usb
  ffmpeg:
    hwaccel_args:
      - -c:v
      - h264_mmal
  cameras:
    # --
    porch:
      ffmpeg:
        inputs:
        - path: ${PORCH_CAMERA}
          roles:
          - detect
      height: 720
      width: 960
      fps: 5
ingress:
   hosts:
      - frigate.${DOMAIN}