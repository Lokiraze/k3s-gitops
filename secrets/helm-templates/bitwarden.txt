bitwardenrs:
  domain: https://bitwarden.$DOMAIN
  admin:
    existingSecret:
      enabled: true
      name: bitwarden-admin-token
      tokenKey: token
ingress:
  hosts:
    - host: bitwarden.$DOMAIN
  tls:
  - hosts:
    - bitwarden.$DOMAIN