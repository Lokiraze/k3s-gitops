bitwarden:
  domain: https://bitwarden.$DOMAIN
  admin:
    existingSecret:
      enabled: true
      name: bitwarden-admin-token
      tokenKey: token
ingress:
  hosts:
    - bitwarden.$DOMAIN
  tls:
  - hosts:
    - bitwarden.$DOMAIN
env:
  DOMAIN: 
  ADMIN_TOKEN: $BITWARDEN_ADMIN_TOKEN