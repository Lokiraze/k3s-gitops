adminPassword: "${GRAFANA_PASSWORD}"
ingress:
  hosts:
    - "grafana.${DOMAIN}"
  tls:
    - hosts:
      - "grafana.${DOMAIN}"
grafana.ini:
  server:
    root_url: "https://grafana.${DOMAIN}"
  auth.generic_oauth:
    enabled: true
    name: "Login"
    client_id: "grafana"
    client_secret: "${GRAFANA_CLIENT_SECRET}"
    auth_url: "https://sso.${DOMAIN}/auth/realms/home/protocol/openid-connect/auth"
    token_url: "https://sso.${DOMAIN}/auth/realms/home/protocol/openid-connect/token"
    api_url: "https://sso.${DOMAIN}/auth/realms/home/protocol/openid-connect/userinfo"
    allow_sign_up: false
    role_attribute_path: "contains(roles[*], '/grafana-admin') && 'Admin' || contains(roles[*], 'editor') && 'Editor' || 'Viewer'"
    email_attribute_name: email
    scopes: "openid profile email"