env:
  POSTGRES_PASSWORD: ${POSTGRES_RECIPES_PASSWORD}
  SECRET_KEY: ${RECIPES_SECRET}
ingress:
  hosts:
    - host: recipes.$DOMAIN
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
      - recipes.$DOMAIN