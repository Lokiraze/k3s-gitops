qbittorrent:
  ingress:
    hosts: 
      - host: torrents.$DOMAIN
        paths:
          - path: /
            pathType: Prefix
    tls:
    - hosts:
      - torrents.$DOMAIN
    
  openvpn:
    vpnConf: |-
      ${VPN_CONF}
    auth: ${VPN_AUTH}
