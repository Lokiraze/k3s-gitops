ingress:
  main:
    hosts: 
      - host: torrents.$DOMAIN
        paths:
          - path: /
    tls:
    - hosts:
      - torrents.$DOMAIN
addons:
  vpn:
    openvpn:
      auth: $VPN_AUTH
    configFile: |-$VPN_CONF
