qbittorrent:
  ingress:
    hosts: 
      host: torrents.${DOMAIN}
      paths:
        - "/"
    tls:
      - hosts:
          - torrents.${DOMAIN}
    
  openvpn:
    vpnConf: |-
      ${VPN_CONF}
    auth: ${VPN_AUTH}
