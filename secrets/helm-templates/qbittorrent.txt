qbittorrent:
  ingress:
    hosts: 
      - host: torrents.$DOMAIN
        paths:
          - path: /
    tls:
    - hosts:
      - torrents.$DOMAIN
    
  openvpn:
    vpnConf: |-
      ${VPN_CONF}
    auth: ${VPN_AUTH}
