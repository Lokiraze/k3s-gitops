---
apiVersion: v1
kind: Service
metadata:
  name: kavita
  namespace: home
  labels:
    app.kubernetes.io/name: kavita
    app.kubernetes.io/instance: kavita
spec:
  selector:
    app.kubernetes.io/instance: kavita
    app.kubernetes.io/name: kavita
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: http