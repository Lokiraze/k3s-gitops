apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: recipes-media-pvc
  namespace: recipes
spec:
  resources:
    requests:
      storage: "1Gi"
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn